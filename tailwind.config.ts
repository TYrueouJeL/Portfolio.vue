export default {
    theme: {
            extend: {
                fontFamily: {
                    sans: ['"JetBrains Mono"', 'monospace'],
                    terminal: ['"VT323"', 'monospace'],
            }
        }
    }
}